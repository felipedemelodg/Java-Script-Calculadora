import { el } from "./el.js";
import { exibir } from "./funcao.js"

el.zero.addEventListener('click', () => {
    exibir(el.zero.innerText)
})
el.um.addEventListener('click', () => {
    exibir(el.um.innerText)
})
el.dois.addEventListener('click', () => {
    exibir(el.dois.innerText)
})
el.tres.addEventListener('click', () => {
    exibir(el.tres.innerText)
})
el.quatro.addEventListener('click', () => {
    exibir(el.quatro.innerText)
})
el.cinco.addEventListener('click', () => {
    exibir(el.cinco.innerText)
})
el.seis.addEventListener('click', () => {
    exibir(el.seis.innerText)
})
el.sete.addEventListener('click', () => {
    exibir(el.sete.innerText)
})
el.oito.addEventListener('click', () => {
    exibir(el.oito.innerText);
})
el.nove.addEventListener('click', () => {
    exibir(el.nove.innerText)
})
el.ponto.addEventListener('click', () => {
    exibir(el.ponto.innerText)
})

el.adicao.addEventListener('click', () => {
    exibir(el.adicao.innerText)
})
el.subtrair.addEventListener('click', () => {
    exibir(el.subtrair.innerText)
})
el.multiplicar.addEventListener('click', () => {
    exibir(el.multiplicar.innerText)
})
el.dividir.addEventListener('click', () => {
    exibir(el.dividir.innerText)
})
el.igual.addEventListener('click', () => {
    exibir()
})